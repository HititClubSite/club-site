const login=document.querySelector('#login'),editor=document.querySelector('#editor');login.addEventListener('submit',async e=>{e.preventDefault();const r=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:new FormData(login).get('password')})});if(r.ok){login.hidden=true;editor.hidden=false;load()}});async function load(){const s=await (await fetch('/api/site')).json();for(const k of ['whatsappUrl','universityUrl','welcomeTemplate'])document.querySelector(`[name=${k}]`).value=s[k]||'';document.querySelector('[name=events]').value=JSON.stringify(s.events||[],null,2);const l=await (await fetch('/api/admin/logs')).json();document.querySelector('#logs').textContent=JSON.stringify(l,null,2)}document.querySelector('#settings').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target);await fetch('/api/admin/settings',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({whatsappUrl:f.get('whatsappUrl'),universityUrl:f.get('universityUrl'),welcomeTemplate:f.get('welcomeTemplate'),events:JSON.parse(f.get('events')||'[]')})});alert('Kaydedildi')});
